#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 * Return: returns 0
 */

int main(void)
{
	char alph = 'a';

	for (alph = 'a'; alph <= 'z'; alph++)
	{
		putchar(alph\n);
	}
	return (0);
}
